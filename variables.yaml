- name: variables demo
  hosts: localhost
  vars:
    trainer: "sivkumar"
    course: "devops"
  tasks: 
    - name: check varibales
      vars:
        course: "AWS"
        trainer: "jawad"
      ansible.builtin.debug:
        msg: "Hello I am learnign {{course}} by {{trainer}}"